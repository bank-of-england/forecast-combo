"""Shiny dashboard for inspecting estimated combination weights."""

from forecast_combo._optional import require_optional_dependency

require_optional_dependency("shiny", "dashboard", "The dashboard")
